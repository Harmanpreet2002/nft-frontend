import { styled } from "styled-components";

const Form = styled.form`
    display: flex;
    flex-direction: column;
    gap: 1em;

    > div {
        display: flex;
        gap: 1em;
    }
`

export { Form };